{% comment %} Shared styles - prepended to all layouts {% endcomment %}

<style>
  /* Custom progress bar - uses TRMNL bg-- classes for dithering support */
  .progress-bar { height: 24px; border-radius: 4px; overflow: hidden; position: relative; }
  .progress-bar--small { height: 18px; }
  .progress-bar--large { height: 36px; border-radius: 6px; }
  .progress-bar--xl { height: 48px; border-radius: 8px; }
  .progress-bar--medium { height: 28px; border-radius: 5px; }
  .progress-fill { height: 100%; }
  .progress-value { position: absolute; right: 8px; top: 50%; transform: translateY(-50%); font-size: 12px; }

  /* Bar gauge layout utilities */
  .justify--between { justify-content: space-between; }
  .justify--center { justify-content: center; }
  .px--4 { padding-left: 32px; padding-right: 32px; }
  .px--3 { padding-left: 24px; padding-right: 24px; }
  .px--2 { padding-left: 16px; padding-right: 16px; }

  /* Compact table styling */
  .table--compact { font-size: 12px; line-height: 1.2; }
  .table--compact th, .table--compact td { padding: 2px 4px; }
  .py--0 { padding-top: 2px; padding-bottom: 2px; }
  .px--1 { padding-left: 4px; padding-right: 4px; }
  .pct--0 { width: 0%; } .pct--5 { width: 5%; } .pct--10 { width: 10%; } .pct--15 { width: 15%; } .pct--20 { width: 20%; }
  .pct--25 { width: 25%; } .pct--30 { width: 30%; } .pct--35 { width: 35%; } .pct--40 { width: 40%; } .pct--45 { width: 45%; }
  .pct--50 { width: 50%; } .pct--55 { width: 55%; } .pct--60 { width: 60%; } .pct--65 { width: 65%; } .pct--70 { width: 70%; }
  .pct--75 { width: 75%; } .pct--80 { width: 80%; } .pct--85 { width: 85%; } .pct--90 { width: 90%; } .pct--95 { width: 95%; } .pct--100 { width: 100%; }
  /* Base hexagon styles - uses TRMNL bg-- classes for dithering support */
  .hexagon { clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%); }
  .hexagon.status-critical { position: relative; }
  .hexagon.status-critical::before { content: ''; position: absolute; top: 3px; right: 3px; bottom: 3px; left: 3px; background: var(--color-white, #fff); clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%); }
  .hexagon.status-critical .hex-label { position: relative; z-index: 1; }
  .hexagon.placeholder { visibility: hidden; }
  .hex-label { line-height: 1.2; word-break: break-all; font-weight: bold; font-family: 'NicoBold-Regular', sans-serif; }

  /* SVG text inherits pixel fonts */
  svg text { font-family: inherit; }

  /* Full layout - dynamic sizing based on stat count */
  .honeycomb { display: flex; flex-wrap: wrap; justify-content: center; align-content: center; width: 100%; height: 100%; }
  .hex-row { display: flex; justify-content: center; width: 100%; }
  .hex-row:last-child { margin-bottom: 0; }

  /* XL: <=10 stats, 5 per row */
  .hex--full-xl .hexagon { width: 100px; height: 115px; }
  .hex--full-xl .hex-label { font-size: 11px; }
  .hex--full-xl .hex-row { margin-bottom: -29px; }
  .hex--full-xl .hex-row:nth-child(even) { transform: translateX(50px); }

  /* LG: 11-20 stats, 6 per row */
  .hex--full-lg .hexagon { width: 75px; height: 86px; }
  .hex--full-lg .hex-label { font-size: 10px; }
  .hex--full-lg .hex-row { margin-bottom: -22px; }
  .hex--full-lg .hex-row:nth-child(even) { transform: translateX(37px); }

  /* MD: 21-35 stats, 7 per row */
  .hex--full-md .hexagon { width: 60px; height: 69px; }
  .hex--full-md .hex-label { font-size: 9px; }
  .hex--full-md .hex-row { margin-bottom: -17px; }
  .hex--full-md .hex-row:nth-child(even) { transform: translateX(30px); }

  /* SM: >35 stats, 11 per row */
  .hex--full-sm { transform: translateX(-16px); }
  .hex--full-sm .hexagon { width: 65px; height: 75px; }
  .hex--full-sm .hex-label { font-size: 11px; }
  .hex--full-sm .hex-row { margin-bottom: -19px; }
  .hex--full-sm .hex-row:nth-child(even) { transform: translateX(32px); }

  /* Half-horizontal layout sizes */
  .hex--hh-lg { transform: translateX(-20px); }
  .hex--hh-lg .hexagon { width: 80px; height: 92px; }
  .hex--hh-lg .hex-label { font-size: 11px; }
  .hex--hh-lg .hex-row { margin-bottom: -23px; }
  .hex--hh-lg .hex-row:nth-child(even) { transform: translateX(40px); }

  .hex--hh-md { transform: translateX(-17px); }
  .hex--hh-md .hexagon { width: 70px; height: 81px; }
  .hex--hh-md .hex-label { font-size: 10px; }
  .hex--hh-md .hex-row { margin-bottom: -20px; }
  .hex--hh-md .hex-row:nth-child(even) { transform: translateX(35px); }

  .hex--hh-sm { transform: translateX(-15px); }
  .hex--hh-sm .hexagon { width: 60px; height: 69px; }
  .hex--hh-sm .hex-label { font-size: 9px; }
  .hex--hh-sm .hex-row { margin-bottom: -17px; }
  .hex--hh-sm .hex-row:nth-child(even) { transform: translateX(30px); }

  /* Half-vertical layout sizes */
  .hex--hv-lg { transform: translateX(-22px); }
  .hex--hv-lg .hexagon { width: 90px; height: 104px; }
  .hex--hv-lg .hex-label { font-size: 11px; }
  .hex--hv-lg .hex-row { margin-bottom: -26px; }
  .hex--hv-lg .hex-row:nth-child(even) { transform: translateX(45px); }

  .hex--hv-md { transform: translateX(-19px); }
  .hex--hv-md .hexagon { width: 75px; height: 86px; }
  .hex--hv-md .hex-label { font-size: 10px; }
  .hex--hv-md .hex-row { margin-bottom: -22px; }
  .hex--hv-md .hex-row:nth-child(even) { transform: translateX(37px); }

  .hex--hv-sm { transform: translateX(-15px); }
  .hex--hv-sm .hexagon { width: 60px; height: 69px; }
  .hex--hv-sm .hex-label { font-size: 9px; }
  .hex--hv-sm .hex-row { margin-bottom: -17px; }
  .hex--hv-sm .hex-row:nth-child(even) { transform: translateX(30px); }

  /* Quadrant layout sizes */
  .hex--q-lg { transform: translateX(-17px); }
  .hex--q-lg .hexagon { width: 70px; height: 81px; }
  .hex--q-lg .hex-label { font-size: 10px; }
  .hex--q-lg .hex-row { margin-bottom: -20px; }
  .hex--q-lg .hex-row:nth-child(even) { transform: translateX(35px); }

  .hex--q-md { transform: translateX(-15px); }
  .hex--q-md .hexagon { width: 60px; height: 69px; }
  .hex--q-md .hex-label { font-size: 9px; }
  .hex--q-md .hex-row { margin-bottom: -17px; }
  .hex--q-md .hex-row:nth-child(even) { transform: translateX(30px); }

  .hex--q-sm { transform: translateX(-12px); }
  .hex--q-sm .hexagon { width: 50px; height: 58px; }
  .hex--q-sm .hex-label { font-size: 8px; }
  .hex--q-sm .hex-row { margin-bottom: -15px; }
  .hex--q-sm .hex-row:nth-child(even) { transform: translateX(25px); }
</style>
